#include <iostream>

int main(int argc, char* argv[]) {
    std::cout << "Luminumbra Server" << std::endl;
    // In the future, this will start the host manager and listen for connections.
    return 0;
}