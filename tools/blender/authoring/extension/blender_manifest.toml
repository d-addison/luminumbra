schema_version = "1.0.0"
id = "luminumbra_geometry_author"
version = "0.2.0"
name = "Luminumbra Geometry Author"
tagline = "Build marked geometry assets with installed engine tools"
maintainer = "David Addison"
type = "add-on"
blender_version_min = "5.1.0"
blender_version_max = "5.1.1"
license = ["SPDX:MIT"]

[permissions]
files = "Capture geometry snapshots and use the configured local authoring service"
