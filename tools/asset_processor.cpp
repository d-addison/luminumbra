#include <iostream>

int main(int argc, char* argv[]) {
    std::cout << "Luminumbra Asset Processor" << std::endl;
    std::cout << "No assets to process." << std::endl;
    // In the future, we will parse command-line arguments here
    // and process game assets.
    return 0;
}