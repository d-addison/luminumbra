# Build outputs
build/
out/
cmake-build-*/
# Transient build/run log dumps written to the repo root during dev iteration
/build_*.txt
/tl_run.log
/wl_run.log
CMakeFiles/
CMakeCache.txt
compile_commands.json

# Generated assets
data/**/*.lmesh

# Runtime and local state
imgui.ini
*.log
*.tmp
*.bak
*.swp
*.swo

# IDE and editor state
.vs/
.vscode/
*.user
*.suo
*.VC.db

# Tool caches
.cache/
.clangd/
.ninja_deps
.ninja_log

# Forge transient state
.forge/hooks/
.forge/output.json
.forge/trace-log.jsonl
.forge/trace-log.jsonl.lock
.forge/tmp/
# Transient build/run capture logs dumped during dev iteration (not source of truth)
.forge/artifacts/

# Local-only project data
/worlds/saves/
/docs/internal/

# OS files
.DS_Store
Thumbs.db

# Steamworks SDK (NOT redistributable -- owner-provided, never commit)
vendor/steamworks/
